# 개선/할 일 백로그 (IMPROVEMENTS)

> 작업하다 눈에 띈 개선점·미완 사항을 모아두는 파일. 나중에 묶어서 한 번에 처리.
> ROADMAP.md(단일 진실 공급원)와 별개의 "메모/백로그". 처리하면 체크.
> 핵심 철학: 정적인 텍스트/블로그를 넘어, 해외에서도 위치 체감이 직관적인 '동적 지도 앱' 구축. 불필요한 정보는 과감히 덜어내고 핵심에 집중!

> **우선순위 원칙 (26/06/17 재정렬)**
> 1순위 = **기술적 충돌·의존성 해소**: 순서를 틀리면 재작업·데이터 손상·회귀가 나는 항목을 먼저.
> 2순위 = **중복 작업 방지**: 같은 파일·영역을 여러 번 건드리지 않도록 관련 작업을 한 사이클(PR)로 묶음.
> 이후 = 독립 개선(P3) → Phase 2.

---

## ⏸️ 다음 세션 착수 (보류 · 설계/결정 완료 — 26/06/25 체크포인트)

### A. ✅ 국내 PC 마이페이지 → 리스트 패널 탭 통합 (글로벌식) — 완료 (26/06/26)
*신규 상태 `leftTab: 'list' | 'mypage'`. 좌측 패널 상단 `목록 | 마이페이지` 탭 추가, 우측 프로필 카드(~345줄) → 마이페이지 탭으로 통째 이전(드롭다운 토글 → 정적 헤더), 우측 카드 제거(필터 카드는 우측 잔류). 데이터 로드 useEffect를 `leftTab` 의존으로 전환, 모든 상세 진입(openDetail)에 `setLeftTab('list')` 추가(마커 클릭 시 상세 가림 방지), 익명 비밀번호 미설정 안내를 마이페이지 탭으로 라우팅, 미사용 `showProfileCard` 제거. tsc·next build 클린. 모바일(하단 네비 → `/profile`)은 무손상.*
- 선행 완료: 국내 리스트 `즐겨찾기` 메인 탭 제거(`1e4249d`), PC 즐겨찾기 표시 복구(`5cc3608`).

### B4. ✅ 증류소 '증류소 한정' 보틀 — 완료 (26/06/26)
*신규 테이블 `global.distillery_bottles`(사진+제품명) + `distillery_bottle_votes`(availability 있어요/없어요, worth 꼭사야해/굳이 · 1인 1표 PK · 보낸 축만 upsert). 마이그레이션 0012. API `places/[id]/bottles`(GET 집계+myVote·notReady처리/POST/DELETE)·`bottles/[id]/vote`(POST/DELETE 철회). 컴포넌트 `GlobalDistilleryBottles`(등록폼 제품명+사진1, 리스트, 토글 투표 낙관적 업데이트), GlobalPlaceDetail 플레이스홀더 → `증류소 한정` 섹션으로 교체. tsc·build 클린.*
- ⚠ **운영DB에 마이그레이션 0012 적용 필요**(로컬/프로드 둘 다). 미적용 시 GET이 `notReady`로 빈 목록(상세 안 깨짐).

---

## 🔴 P1 — 기술적 충돌·의존성 (먼저 설계·해소)
*잘못된 순서로 착수하면 재작업·데이터 손상·회귀 발생. 착수 전 반드시 해소. (NaverMap은 P1급 리스크지만 **실행은 최후** — 맨 아래 별도 섹션)*

- [ ] **(선행 게이트) 디자인시스템 토대 push → PR → main 머지**: 토큰·공통 프리미티브·위반/a11y 수정 + `.claude/DESIGN_SYSTEM.md` 추적이 `worktree-design-system-rollout` branch에 **로컬 커밋 완료**(`c18736e`, 아직 미push). 남은 건 push → PR → 머지뿐. **이 머지가 모든 UI 적용 항목의 선행조건** — 머지 전 컴포넌트 적용 착수 시 충돌·재작업.
- [ ] **평가 시스템 마이그레이션 = DB 작업 (UI 아님 · 데이터 손상 위험)**: 국내 `comments`+`reactions(재방문)` ↔ 글로벌 `reviews.rating` 구조가 상이.
  - 선행: 국내 리뷰 테이블에 **nullable `rating`** 컬럼 추가 설계 → 기존 재방문 데이터는 `rating=null`로 **보존**, 별점 분포 집계에서 제외·카드 미표시(추론/왜곡 금지).
  - 차용 스펙: 3단 세그먼트 `★별로/★★무난/★★★최고`(별+단어) + 분포 집계 바 + 리뷰별 유용해요 투표 + 맥락형 placeholder(평점 'meh'면 "아쉬웠던 점"). (`GlobalReviewForm.tsx` L16·L218 · `GlobalPlaceDetail.tsx` L545 · `GlobalMyRecords.tsx` L306)
  - ⚠ 배포 전 **마이그레이션 검증 체크리스트**(데이터 보존·롤백) 필수.
- [ ] **⚠ 흡연 ↔ 콜키지 (후기폼 이식과 반드시 동시 처리)**: 글로벌 후기폼엔 흡연·커버차지 입력이 내장(`GlobalReviewForm.tsx` L389~420). 국내 이식 시 *같은 작업에서* **흡연 속성 제거 + 국내 핵심 '콜키지 여부' 추가**. 따로 쪼개면 흡연 UI가 국내로 새는 회귀 발생.
> ↪ **NaverMap 레이아웃**은 P1급 리스크이나 *실행은 최후* → 맨 아래 "⛔ 실행 최후" 섹션 참조.

---

## 🟠 P2 — 중복 작업 방지 (한 파일·영역을 한 사이클로 묶음)
*상세 페이지·버튼·피드백·용어를 따로 건드리면 같은 파일을 N번 수정. 아래는 "동일 PR"로 묶어 1회 처리.*

- [ ] **국내 상세 페이지 전면 개편 (앵커 작업 — 하위 전부 동시에)**: 국내 `PlaceDetailClient`를 글로벌 체계(`GlobalReviewForm`·`GlobalPlaceDetail`·`GlobalMyRecords`)로 한 번에 개편. (단순 댓글 섹션 → 후기 체계로 교체)
  - [ ] **하단 레이아웃 통합**: 태그/사진/구매인증/후기 컴포넌트 이식 + P1 평가 시스템 + 흡연→콜키지 반영.
  - [ ] **바틀 구매 인증 가시성**: 가독성 낮은 구매처/인증 영역을 `DESIGN_SYSTEM.md` 기준 리팩토링.
  - [ ] **액션 버튼 통일**: 유용해요/유용하지않아요·수정·삭제·신고를 글로벌 가시성 기준 + 터치 44px + `ui/` 프리미티브(Button/Tag/Badge) 조립.
  - [ ] **피드백 적용**: Skeleton(로딩)/Toast(성공·실패)/Empty(빈 결과) 공통 컴포넌트 적용(무반응 금지).
  - [ ] **뱃지 배치 규칙**: 속성 뱃지(예약필수·면세)는 항상 종류 뱃지(증류소/식당/바) 우측.
- [ ] **불필요한 정보·탭 정리 (상세 개편과 동시)**: '최근 관찰' UI 제거 / '교통·접근' 탭 제거(길찾기는 구글 지도) / '예약 란' 탭 → '공식 사이트'로 통합하되 **'예약 필수' 뱃지는 유지**(공식사이트 링크 옆) / 효용 낮은 '구매 팁' 탭 삭제 / 리스트 단계 기여자 표시 제거(상세에만).
- [ ] **용어·명칭 일원화 (전 화면 일괄)**: '장소 추가'(국내)↔'장소 등록'(글로벌), '지도 보기' 등 동일 기능 텍스트 통일. 상세 개편과 같은 사이클에.

---

## ⛔ 실행 최후 (P1급 리스크 · 다른 모든 항목 완료 후 단독)
*우선도는 고위험(P1)이지만, 단일 4,095줄 파일이라 다른 저위험 항목을 모두 끝낸 뒤 단독 PR로 착수.*

- [ ] **NaverMap 레이아웃 개편 (`src/components/NaverMap.tsx` · 4,095줄)**:
  - [ ] PC 더블패널: 리스트 좌측 고정 + 상세 우측 지도 위 플로팅.
  - [ ] 모바일: 좌측 플로팅 패널 → 바텀시트 일괄 전환(신규 `ui/BottomSheet` 활용).
  - [ ] **핀 빈공간 중앙정렬 / 네이버 센터 보정(두 패널에 안 가려지는 실가시 영역 중앙 카메라)은 절대 유지** (글로벌 Embed API 한계 내).
- [ ] **하드코딩 일괄 제거 패스 (모든 개선 작업 종료 후 — 진짜 마지막)**: `DESIGN_SYSTEM.md` 하드코딩 금지 규칙을 국내·글로벌 전체에 *한 번에* 적용 — 남은 `gray-*`/인라인 hex/임의값을 토큰으로 치환. ⚠ **개선 작업 중에는 이 규칙을 머지 게이트로 쓰지 않음**(글로벌 기존 쿨그레이 포함, 여기서 일괄 정리). 예외 3가지(SVG/canvas·서드파티·알파합성)만 주석 남기고 허용.

---

## 🟡 P3 — 독립 개선 (충돌·중복 적음, 병렬 가능)

- [x] **데이터 수집 자동화** (26/06/25, `45d2043`): 공식 사이트만 자동채움(websiteUri, Enterprise SKU). 영업시간은 범위 제외.
- [x] **Google place_id 저장** (26/06/25, `8e19db5`): `google_place_id` 컬럼(마이그레이션 0010) + Embed 핀을 `q=place_id:` 로 정확화. 중복판정은 원어로 대체돼 딥링크/핀 용도.
- [x] **다중 속성 필터 검색** (26/06/25, `d4c2621`): 국기 칩·속성 칩(다중 AND, 데이터 존재분만)·도시 칩 + 세부필터 접기/유형칩 토글.
- [x] **한글명 입력 보조** (26/06/25, `ade7429`): 구글 `languageCode:ko` mainText를 이름 칸에 칩 제안.
- [x] **검색 실패 표시** (26/06/25, `ade7429`): 실패/네트워크오류/결과없음 각각 명시.
- [x] **회원 탈퇴 (국내/글로벌 공통)** (26/06/25, `dbd8722`): 익명화 정책 + service-role 라우트 + 재확인 모달 + 마이그레이션 0009. ⚠ 운영엔 Vercel `SUPABASE_SERVICE_ROLE_KEY` 필요.

---

## 🔵 Phase 2 (코어 지도/탐색 안정화 이후)

- [ ] **커뮤니티 정정/보강 (위키 방식)**: 속성·상세(증류소 투어명/가격대/특이사항 등)를 타인이 보강/수정. `history` 테이블로 수정 이력 로깅(추적/롤백). 볼륨 큼 → 코어 후속.
- [ ] **게이미피케이션**: 활동(등록·수정·후기) 기반 레벨/뱃지 + 후기·댓글 뱃지 노출 + '이달의 기여자' 리더보드.
- [ ] **여행 플래너(직선거리 기반)**: 날짜 슬롯에 장소 담아 슬롯 간 직선거리 표시(실 길찾기는 구글 지도 아웃링크).
- [ ] **국가 개요 뷰 핀 클러스터**: 국가 내 장소 분포 파악용 핀 표시.

---

## 🟢 완료 / 운영 잡무

- [x] **eslint 깨진 의존성 복구 + NaverMap any 18개 전면 수정** (26/06/16)
- [x] **디자인시스템 토대** (branch `worktree-design-system-rollout` 로컬 커밋 `c18736e` · push/머지 대기, 26/06/17): `globals.css` 토큰 정의·정합(semantic-error/scrim/border-focus/ease 등), 공통 프리미티브 `src/components/ui/`(Button·Input·Textarea·Tag·Badge·Skeleton·Toast·EmptyState·BottomSheet), 국내 브랜드색 토큰화, 위반 제거(임의 z·`text-[9px]`), a11y `:focus-visible` 베이스라인, 글로벌 위반 수정. → P1 "토대 머지" 게이트로 이어짐.
- [x] **국내 보틀 구매 인증 (기능 동기화)** (branch `worktree-design-system-rollout`, **미커밋**, 26/06/17): 글로벌 구매 인증을 국내 리쿼샵(`type='whisky'`)에 이식. 신규 `bottle_logs` 테이블(Supabase 생성됨) + API `app/api/places/[id]/bottle-logs`(GET/POST/DELETE, 인증 SSR 클라이언트로 insert) + `src/components/PurchaseForm.tsx`(`ui/` 프리미티브) + 상세 패널 섹션. UI 대공사·후기는 미실시(스코프 한정).
  - ⚠ **PC·모바일 상세가 2벌로 중복**임을 재확인 — 같은 섹션을 **`NaverMap` PC 패널**과 **`PlaceDetailClient` 모바일** 양쪽에 각각 넣어야 했음. → P1 "NaverMap PC 더블패널 통일" + P2 "상세 페이지 전면 개편(중복 제거)" 우선도의 실증 근거.
  - DB: `reviews`·`review_votes` 테이블도 생성돼 있으나 **이번 스코프 미사용**. 평가 시스템(P1) 착수 시 `supabase/migrations/0009`(comments.rating, 구 방식)와의 충돌 정리 필요.
- [x] **글로벌 상세 정보란 개편 B1~B3** (26/06/25): B1 공식사이트 표시+타입별 정보란 재구성(`95215b8`) / B2 리쿼샵 후기 시음·면세+집계(`ae71a79` — reviews에 shop_had_tasting·shop_tax_free 컬럼 raw SQL로 추가, 별도 마이그레이션 파일 없음) / B3 증류소 구매팁 방명록(`e36d5fe`, 마이그레이션 0011 purchase_tips). B4(증류소 한정)는 분리.
- [x] **글로벌 마이페이지 탭 통합**(`d4c2621`) + **중복 등록 원어 기준**(`b6f1fd0`) + **탈퇴 모달 Portal/z**(`b08420c`) (26/06/25).
- [ ] **Vercel 환경변수 등록**: `GOOGLE_PLACES_API_KEY`, `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY`, **`SUPABASE_SERVICE_ROLE_KEY`(탈퇴 작동 필수)**.
- [ ] **Embed 키 리퍼러 배포 도메인 추가**
- [ ] **데이터 삭제분 재등록 검증**
- [ ] **적용한 SQL 마이그레이션 운영DB 반영 확인**: 0009(탈퇴 익명화)·0010(place_id)·0011(purchase_tips)·**0012(distillery_bottles+votes)** + reviews shop_had_tasting/shop_tax_free. (0012는 로컬·운영 **둘 다 미적용** — 적용 전엔 증류소 한정 섹션이 notReady 빈 목록)
